model: ../recurrentgemma_2b.py
baseline: compiled
use_library_inference: true
workloads:
- name: recurrentgemma_2b_prefill_2048
  inputs:
  - shape:
    - 1
    - 2048
    dtype: int32
    low: 0
    high: 256000
budget:
  per_region: 8
  total: 24
final_benchmark:
  # Complete an MLX-LM request for one token, starting from an empty cache.
  steps: 1
  pairs: 4
  warmup_steps: 3
