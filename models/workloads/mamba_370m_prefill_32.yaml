model: ../mamba_370m.py
baseline: compiled
use_library_inference: true
workloads:
  - name: prefill
    context: 0
    inputs:
      - shape: [1, 32]
        dtype: int32
        low: 0
        high: 50280
budget:
  per_region: 8
  total: 24
final_benchmark:
  # Complete an MLX-LM request for one token, starting from an empty cache.
  steps: 1
  pairs: 8
  warmup_steps: 3
